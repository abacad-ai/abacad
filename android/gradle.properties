org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
# Jetpack Compose (the setup/awareness UI) requires AndroidX. The device-control
# core still uses framework APIs + OkHttp; only the UI layer pulls in AndroidX.
android.useAndroidX=true
kotlin.code.style=official
